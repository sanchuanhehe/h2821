{
  "name": "Near Link SDK hi2821",
  "context": "..",
  "dockerFile": "Dockerfile",
  "workspaceFolder": "/sdk",
  "remoteUser": "root",
  "mounts": [
    "source=${localWorkspaceFolder},target=/sdk,type=bind,consistency=cached"
  ],
  // "postCreateCommand": "ls -al /sdk && if [ ! -f ./build/prebuilts_download.sh ]; then echo 'prebuilts_download.sh not found! Please check your SDK setup.'; else ./build/prebuilts_download.sh; fi && python3 -m pip install --user ./build/hb || (python3 -m pip uninstall -y ohos-build && python3 -m pip install --user ./build/hb)",
  "customizations": {
    "vscode": {
      "extensions": [
        "llvm-vs-code-extensions.vscode-clangd",
        "GitHub.copilot",
        "twxs.cmake",
        "ms-vscode.cpptools",
        "cschlosser.doxdocgen",
        "vinirossa.vscode-gitandgithub-pack",
        "ms-python.debugpy",
        "ms-python.python",
        "luveti.kconfig",
        "intellsmi.comment-translate"
      ]
    }
  }
}